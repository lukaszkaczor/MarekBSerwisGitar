@model IEnumerable<SerwisGitar.Models.DbModels.GuitarPart>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Typ 
        </th>
        <th>
            Nazwa
        </th>
        <th>
            Zdjęcie
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartType.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            <img src="@item.ImageUrl" width="100px"/>
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GuitarPartId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GuitarPartId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GuitarPartId })
        </td>
    </tr>
}

</table>

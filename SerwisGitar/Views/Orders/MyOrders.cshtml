@model List<SerwisGitar.Models.DbModels.Order>

<table class="table">
    <tr>
        <th>
            Numer zamówienia
            @*@Html.DisplayNameFor(model => model.)*@
        </th>
        <th>
            Data
            @*@Html.DisplayNameFor(model => model.Name)*@
        </th>
        <th>
            Status
            @*@Html.DisplayNameFor(model => model.Price)*@
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OrderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderStatus)
            </td>
            <td>
                @Html.ActionLink("Szczegóły", "OrderDetails", new {  id = item.OrderId})
                @*@Html.ActionLink("Details", "Details", new { id=item.ServiceId }) |*@
                @*@Html.ActionLink("Delete", "Delete", new { id=item.ServiceId })*@
            </td>
        </tr>
    }

</table>
